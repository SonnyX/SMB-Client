<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <RootNamespace>SMBLibrary</RootNamespace>
    <AssemblyName>SMBLibrary</AssemblyName>
    <PackOnBuild>true</PackOnBuild>

    <Authors>Randy von der Weide</Authors>
    <PackageId>SMB-Client</PackageId>
    <Product>SMB-Client</Product>
    <RepositoryType>git</RepositoryType>

    <RepositoryUrl>https://github.com/SonnyX/SMB-Client</RepositoryUrl>
    <PackageTags>smb smb-client</PackageTags>
    <Version>1.4.7.1</Version>

    <Description>Free, Open Source, User-Mode SMB 1.0/CIFS, SMB 2.0, SMB 2.1 and SMB 3.0 Client Library</Description>
  </PropertyGroup>

  <PropertyGroup>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    <IncludeSymbols>true</IncludeSymbols>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="ilmerge" Version="3.0.41" PrivateAssets="All" />
    <ProjectReference Include="..\Utilities\Utilities.csproj" PrivateAssets="All" />
  </ItemGroup>

  <Target Name="ILMerge" Condition="'$(Configuration)' == 'Release'" AfterTargets="AfterBuild">
    <Message Text="MERGING: into $(OutputAssembly)" Importance="High" />
    <Exec Command="$(ILMergeConsolePath) /out:$(OutputPath)\\$(AssemblyName).dll $(OutputPath)\\$(AssemblyName).dll $(OutputPath)\\Utilities.dll" />
  </Target>
</Project>